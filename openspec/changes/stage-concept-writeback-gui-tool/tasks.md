## 1. TDD contract tests for staged CLI behavior

- [x] [P] 1.1 Add a failing test in `test/wiki-concept-resume.test.js` for `REQ-WI-020 Post-compile Joplin Data API writeback`, `Staged concept resume output`, and `Decision 1: Concept resume never writes to Joplin`: `wiki-compile --resume-stage concepts` with writeback enabled writes local concepts/index, returns `writeback_deferred: true`, and sends no mutating Joplin Data API requests; verify first with `node --test test/wiki-concept-resume.test.js` red.
- [x] [P] 1.2 Add a failing test in `test/wiki-concept-resume.test.js` for `Staged concept telemetry`: concept resume output includes `compile_adapter: "local"`, `resume_stage`, `summary_paths_read`, `changed_summary_paths`, `concept_paths_planned`, `concept_paths_written`, `writeback_relpaths`, and `writeback_deferred`; verify first with `node --test test/wiki-concept-resume.test.js` red.
- [x] [P] 1.3 Add a failing test in `test/joplin-wiki-writeback.test.js` for `Staged concept publish boundary`, `Staged writeback dry-run remains non-mutating`, `Incremental writeback relPath contract`, and `Decision 2: Writeback stage is the publish boundary`: `--resume-stage writeback` dry-run is non-mutating and non-dry-run upserts only completed concepts and All-Concepts relPaths; verify first with `node --test test/joplin-wiki-writeback.test.js` red.
- [x] [P] 1.4 Add failing tests in `test/agent-compile.test.js` for `SCN-WI-STAGED-CONCEPT-AGENT-01`, `SCN-JWKB-STAGED-PUBLISH-01`, `SCN-JWKB-STAGED-DRYRUN-01`, and `Decision 1: Concept resume never writes to Joplin`: `agent-compile --resume-stage concepts` writes only local concept/index files and `agent-compile --resume-stage writeback` is the only agent resume path that mutates Joplin; verify first with `node --test test/agent-compile.test.js` red.
- [x] [P] 1.5 Add a failing test in `test/agent-compile.test.js` for `Staged concept telemetry`: agent resume output includes `compile_adapter: "agent"`, `resume_stage`, `changed_summary_paths`, `concept_paths_written`, `writeback_relpaths`, and `writeback_deferred`; verify first with `node --test test/agent-compile.test.js` red.

## 2. CLI staged concept and writeback implementation

- [x] 2.1 Implement `Decision 1: Concept resume never writes to Joplin` in `src/wiki/wiki-compiler.js`: concept resume non-dry-run produces local `wiki/concepts/*.md` and `wiki/indexes/All-Concepts.md`, returns `writeback_deferred: true`, reports `compile_adapter: "local"`, and does not call `runWikiWriteback`; verify 1.1 and 1.2 turn green with `node --test test/wiki-concept-resume.test.js`.
- [x] 2.2 Implement `Decision 2: Writeback stage is the publish boundary` in `src/wiki/wiki-compiler.js` and `src/joplin/wiki-writeback.js`: writeback resume discovers completed concept/index files and is the only local resume path that mutates Joplin; verify 1.3 turns green with `node --test test/joplin-wiki-writeback.test.js test/wiki-concept-resume.test.js`.
- [x] 2.3 Implement `agent-compile --resume-stage concepts` and `agent-compile --resume-stage writeback` in `src/commands/cmd-agent-compile.js` using the same stage contract as local mode: concept resume invokes local `codex exec` agent writing only wiki files, writeback resume delegates to the shared writeback stage, and telemetry reports `compile_adapter: "agent"`; verify 1.4 and 1.5 turn green with `node --test test/agent-compile.test.js`.
- [x] 2.4 Update CLI help and operator-facing docs in `src/cli.js`, `README.md`, and `docs/llm-knowledge-flow.md` so users see the staged sequence concept dry-run, concept run, writeback dry-run, writeback run for both `wiki-compile` and `agent-compile`; verify with existing CLI help tests plus manual content review for the eight commands.

## 3. Incremental raw-change concept scope

- [x] [P] 3.1 Add failing sqlite-sync tests in `test/sqlite-sync-change-detection.test.js` for `Incremental concept downstream orchestration`, `Incremental concept rebuild from changed summaries`, `Incremental downstream state commit boundary`, `Incremental downstream observability`, `SCN-JSQ-INCREMENTAL-CONCEPT-01`, `SCN-JSQ-INCREMENTAL-CONCEPT-AGENT-01`, and `Decision 3: Incremental raw changes carry summary scope into concept planning`: changed raw updates `summaries/a.md`, rebuilds affected `concepts/topic.md`, writes only downstream relPaths, and commits snapshot only after success for `compile_mode: local` and `compile_mode: agent`; verify first with `node --test test/sqlite-sync-change-detection.test.js` red.
- [x] 3.2 Implement changed summary scope propagation in `src/commands/cmd-sqlite-sync.js`, `src/commands/cmd-agent-compile.js`, and `src/wiki/wiki-compiler.js`: downstream local and agent compile expose `compile_adapter`, `changed_summary_paths`, `concept_paths_written`, and `writeback_relpaths` while excluding unchanged summaries from resume writeback; verify 3.1 turns green.
- [x] 3.3 Implement concept impact expansion in `src/wiki/wiki-planner.js` and `src/wiki/wiki-compiler.js`: changed summaries map to existing concepts through `summary_refs`, `source_refs`, and canonical metadata, with LLM semantic judgment used when direct metadata is insufficient; verify with targeted tests in `test/wiki-concept-resume.test.js` and `test/sqlite-sync-change-detection.test.js`.

## 4. GUI tool actions and visible rename

- [x] [P] 4.1 Add failing GUI tests in `test/health-gui/concept-resume-actions.test.js` for `Joplin-LLM-wiki tool visible identity`, `Concept resume actions in GUI`, `Writeback resume actions in GUI`, `SCN-HGUI-CONCEPT-ACTIONS-AGENT-01`, `SCN-HGUI-WRITEBACK-ACTIONS-AGENT-01`, `GUI concept actions use single-flight guard`, `GUI displays staged concept telemetry`, `Decision 4: GUI wraps fixed local and agent stage actions and keeps single-flight protection`, and `Decision 5: Rename only user-visible GUI branding first`; verify first with `node --test test/health-gui/concept-resume-actions.test.js` red.
- [x] 4.2 Implement fixed GUI stage actions in `src/health-gui/main.js`, `src/health-gui/preload.cjs`, `src/health-gui/corpus/corpus-pipeline-runner.js`, and `src/health-gui/renderer/app.js`: local concept dry-run/run, agent concept dry-run/run, local writeback dry-run/run, and agent writeback dry-run/run spawn the exact argv from specs and share the existing single-flight guard; verify 4.1 action assertions turn green.
- [x] 4.3 Rename user-visible GUI branding in `src/health-gui/main.js`, `src/health-gui/renderer/index.html`, `src/health-gui/renderer/app.js`, and docs: window title and primary heading show `Joplin-LLM-wiki tool` while existing executable names remain valid; verify 4.1 rename assertions and existing health GUI tests pass.
- [x] 4.4 Surface parsed staged telemetry in GUI operation results: renderer displays or exposes `compile_adapter`, `resume_stage`, `changed_summary_paths`, `concept_paths_written`, `writeback_relpaths`, `writeback_collision_count`, and `writeback_orphan_candidate_count`; verify 4.1 telemetry assertions turn green.

## 5. Documentation and configuration guidance

- [x] [P] 5.1 Update `README.md`, `docs/llm-knowledge-flow.md`, and `docs/scheduling-examples.md` for the staged product behavior: concept compile completes locally before Joplin writeback in local and agent modes, raw-change incremental concept rebuild writes only changed downstream relPaths, and GUI provides equivalent operations; verify by content review against SCN-WI-STAGED-CONCEPT-01, SCN-WI-STAGED-CONCEPT-AGENT-01, SCN-JWKB-STAGED-PUBLISH-01, SCN-JSQ-INCREMENTAL-CONCEPT-01, SCN-JSQ-INCREMENTAL-CONCEPT-AGENT-01, SCN-HGUI-CONCEPT-ACTIONS-01, SCN-HGUI-CONCEPT-ACTIONS-AGENT-01, and SCN-HGUI-RENAME-01.
- [x] [P] 5.2 Update `config.yaml.example` comments only if needed to clarify that resume stage behavior is CLI/GUI controlled and no Chroma, embeddings, or remote services are added; verify `node --test test/config-schema.test.js` remains green.

## 6. Verification and Spectra checks

- [x] 6.1 Run targeted tests for the implementation contract: `node --test test/wiki-concept-resume.test.js test/agent-compile.test.js test/joplin-wiki-writeback.test.js test/sqlite-sync-change-detection.test.js test/health-gui/concept-resume-actions.test.js test/health-gui/corpus-pipeline-runner.test.js test/health-gui/raw-wiki-health.test.js`; acceptance is exit code 0.
- [x] 6.2 Run full regression `pnpm test`; acceptance is exit code 0 with no failed tests.
- [x] 6.3 Run `spectra validate stage-concept-writeback-gui-tool` and `spectra analyze stage-concept-writeback-gui-tool --json`; acceptance is valid artifacts and no Critical or Warning findings.
